// Package render was a planned abstraction layer that forwarded to tui.DrawBuffer
// and tui.DrawCursor. Those functions remain the canonical implementations.
// This package is intentionally empty; remove it once a proper render abstraction
// is designed.
package render
